# 开发计划

状态：2026-09-23 已从首轮预览进入完整功能对齐。用户要求 Sub2API 的产品能力均纳入 CPA Cloud；以 [功能矩阵](feature-parity-plan.md) 分批交付，不以打包数量或最小文本请求替代产品验收。

## 当前执行批次

请求治理已进入源码集成：员工、Key 与独立治理组的 RPM/并发硬限制默认关闭，管理网页和持久幂等操作已接线；旧库升级、关闭/开启、超限零派发、取消、重启及撤销的隔离进程验收通过。完整回归结果以[集成状态](integration-status.md)为准。TPM/成本只读 shadow 观测已通过进程、浏览器及 Linux CI 验收；新增默认关闭的固定模型硬预算已完成管理、持久预留、最终派发、原子结算及恢复接线，本批证据见[预算集成进度](budget-service-integration-progress.md)。这仍是有限模型/请求范围的实验，不能把统计或局部上界证明当成通用预算完成。来源与边界见[治理总契约](governance-contract.md)和[管理契约](governance-management-contract.md)。

Responses 首批、Claude/Gemini API Key 原生协议、Codex 网页 OAuth 与会员模型目录已合入当前源码。Codex 共享生命周期现已包含后台自动刷新、请求前刷新和手动刷新，合成测试覆盖来源/client binding 锁定、暂停与修复恢复；该能力仍是默认关闭的源码实验，供应商侧撤销、真实账号及第三方 OAuth 注册均未验收。

四类凭据批量导入及网页已通过真实 Go 服务与浏览器验收。账号分组、渠道、网页池编辑器、权重/优先级、账号级容量、持久化租约、固定 cooldown 和四协议执行已接线；`scripts/smoke-account-pool.mjs` 与 `scripts/smoke-account-pool-ui.cjs` 的真实进程验收通过，该批网页测试为 58 项通过。HMAC 会话按 employee、Key、公开模型和协议入口隔离，账号全局并发与租约重启恢复已有子集测试。本批接入派发前一次安全换号；生成恢复已进入本轮源码集成；供应商配额/余额和通用预算仍待实现；出站代理首批子集见下文。这些结果使用合成凭据和模拟上游。

用量账本核心、服务协调器与四协议 hook 已接入源码；HTTP 用量、SQL 写入失败回滚、取消、重启恢复及完整 Go 回归通过。本批增加管理员用量查询、请求/尝试明细、按账号/实际模型的不可变价格版本和网页“用量与成本”，派发前锁定价格快照。进程验收已覆盖版本幂等、在途改价、账号池实际账号、未知费用、分页与重启。固定模型预算新增共享事务结算；仍没有售价、余额或日/月报表导出，不能把内部估算称为正式计费。接口见 [用量与价格契约](usage-management-contract.md)；完整验收证据见 [集成状态](integration-status.md)。Claude/Gemini 会员保持明确阻塞记录，不能用 API Key 原生通路替代会员支持。

注册、Key/租户限额、套餐、订阅、充值、兑换、支付、多租户隔离、代理池、治理和运维扩展继续按 [功能矩阵](feature-parity-plan.md) 推进，并保留在总目标中。默认内部企业模式仍关闭对外注册和商业化入口；后续必须先完成身份、金额账本、租户隔离、回调安全及恢复验收。

账号换号已按[安全换号契约](account-pool-failover-contract.md)接入源码：仅允许已证明未派发的账号预检失败切换一次，进入模型执行器后禁止自动重放。公共协调器与四协议隔离进程验收已覆盖失败分类、权限/池版本重查、一条父请求、实际账号费用、429 不重放和重启撤销；完整 Go/CI 结果在[集成状态](integration-status.md)持续记录。

本轮按[上游测试与恢复契约](upstream-health-contract.md)接通本地凭据/目录测试、操作查询、冷却展示和手工清除，已通过本地验收。网页明确区分本地可解析、目录可读和生成可用；断线查询原操作，重启不重发，清除按账号 revision 与冷却事件同时校验。默认关闭的生成恢复、精确账号维护租约、隔离与独立用量已接线；它们的独立验收见下文。

[执行拆分](account-recovery-execution-plan.md)中的独立探测账本、精确账号维护租约、恢复隔离、固定协议 runner 和内部事务桥已进入源码集成，见[基础模块契约](system-probe-foundations-contract.md)。本轮按[协调器契约](account-recovery-coordinator-contract.md)接入失败路径快照、默认关闭双门控、单 worker、持久重试/人工阻塞、Codex 同事务刷新版本承接及网页控制。真实临时进程与浏览器验收通过，相关故障回归与最新 CI 证据在集成状态中记录；不等于真实供应商认证或完整恢复调度全部完成。

普通提交只跑相关测试/编译，文档不触发安装构建；阶段交付验收后再集中发布，不为每个功能生成全平台安装包。

默认关闭的[持久定时测试首批](scheduled-tests-contract.md)已接入源码：可保存凭据检查/模型目录计划，worker 复用既有健康测试路径并持久化有界历史。真实临时子进程与浏览器验收覆盖默认关闭零出站、开启后目录单次执行及重启不重放；这不证明模型生成可用，也不包含通知、cron 或收费生成测试。

[账号与模型生命周期管理](account-lifecycle-management-contract.md)已接入 revision/CAS、墓碑、默认隐藏和上游凭据销毁；上游归档与定时计划停用同事务提交，提交后取消在途定时检查。独立的[加密备份 CLI](backup-restore.md)已覆盖活动 WAL 一致快照、认证加密、只读校验和新目录恢复；它不是自动备份、异机保留或生产灾备编排。

[出站代理首批](outbound-proxy-contract.md)已接入 API Key 生成/目录/测试/恢复与网页，包含版本绑定及持久化仅握手检查；独立证据见[集成状态](integration-status.md)。Codex 全生命周期出口、其他代理协议与自动轮换仍未实现。
[员工治理](governance-contract.md)的 RPM/并发已合入主线且通过轻量 CI；[观测读路径](governance-observation-contract.md)也已通过独立验收与轻量 CI。
治理先接 RPM/并发，Token/成本未知值不当作零；完整硬预算、租户及其余矩阵需求继续保留。测试夹具优化独立审阅，
不得降低生产密码强度或删除认证、迁移、并发断言来缩短验证时间。

以下保留首轮预览范围与验收记录，不能用其范围限制完整计划。

## 部署范围

同一服务支持 Linux/Windows/macOS，运行于云服务器、公司内网主机或个人电脑。
默认 127.0.0.1:8787；回环 HTTP 仅限本机。跨电脑监听要求 HTTPS 或受明确配置的 TLS 入口保护。
员工直接使用 CC Switch/原生工具，无专用客户端依赖，Key 默认永久有效。

## 分工

1. Go 服务端：独立实现持久化、管理员初始化/会话、员工与 Key、上游 API Key、模型路由及 Chat Completions 请求。
2. 网页后台：登录、员工与 Key、上游、模型、状态页面；使用真实 API，不内置假数据。
3. 会员接入调研：ChatGPT/Codex、Claude、Gemini 按官方来源分别核实导入、授权、刷新与调用条件，明确可实现和阻塞部分。
4. 集成与验收：主任务负责契约协调、启动脚本、跨平台构建、模拟上游端到端测试、来源与发布边界。

先实现 API Key 模拟上游闭环，同时完成三家会员接入的可行性报告；会员支持是产品目标，不因预览仅有 API Key 就视为交付完成。

## 预览验收

初始化管理员 → 网页登录 → 添加上游和路由 → 创建员工/永久 Key → 模型请求及 SSE → 撤销拒绝 → 重启恢复。
任何未完成的持久化统计、会员授权、协议兼容和部署安全能力在交付说明中明确标记。

## 协作约束

在 C:\workspace\cpa-cloud 工作；各任务拥有独立文件范围。不得读取归档项目源码，不提交其他任务文件，不发布远程仓库或部署。
接口以 preview-contract.md 为准，变更先报告集成负责人。自主处理例行实现与测试，不等待额外启动许可。
