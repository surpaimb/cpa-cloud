# 核心验收矩阵

全部待实现；通过项需附测试名、环境、版本和证据。

| 编号 | 场景 | 预期 |
| --- | --- | --- |
| A01 | 并发首次初始化 | 只产生一个管理员，初始化凭据不能重复使用 |
| A02 | 员工 Key 调用管理 API | 拒绝；Cookie、CSRF 和员工 Key 边界互不混用 |
| K01 | 创建 Key 并刷新列表 | 仅创建响应有秘密，数据库/日志/列表没有秘密 |
| K02 | 创建响应丢失后重试 operation_id | 不新增 Key，不重显秘密，可撤销旧记录 |
| K03 | 撤销与模型准入并发 | 成功响应后新准入拒绝，已准入流按约定完成 |
| K04 | 停用再恢复员工 | 普通未撤销 Key 恢复；明确撤销 Key 保持失效 |
| K05 | 到期边界、未知 Key、摘要错误 | 拒绝；不以可区分秘密的详情报错 |
| K06 | 数据库提交失败/内存发布失败 | 不虚报变更成功，不产生授权过期窗口 |
| M01 | selected 空集及全部模式新增模型 | 空集无权限；全部模式按约定纳入新模型 |
| M02 | 不同员工获取模型及响应状态对象 | 无越权目录/响应/会话读取 |
| U01 | 上游凭据替换失败 | 旧凭据完整保留，界面不回显秘密 |
| U02 | 上游重定向与内部地址探测 | 不泄露凭据，不访问未授权内部目标 |
| P01 | 同协议非流式/多轮工具调用 | 字段语义与工具关联保持正确 |
| P02 | SSE 慢客户端、断开、取消 | 背压有效、上游取消、资源及时释放 |
| P03 | 上游收请求后断开 | 不自动重复生成；状态和用量标记不确定 |
| P04 | Responses 存储/续接未支持 | 明确拒绝，不能穿透访问共享上游状态 |
| T01 | 重复终态与重复日志恢复 | 每尝试只记一次，客户端请求数不膨胀 |
| T02 | 未收到 usage 的中断流 | NULL/unknown，不误计为零或成功 |
| T03 | 缓存用量归一化 | 不重复相加包含关系字段 |
| T04 | 磁盘满、终态队列满、异常重启 | 就绪降级、风险可见，恢复后幂等处理 |
| D01 | 一致性备份与新机器恢复 | 员工权限、凭据、统计可验证恢复 |
| D02 | 升级失败回滚 | 数据库兼容性与回滚边界有真实证据 |
| C01 | CC Switch 和各工具 | 按工具版本/协议分别记录支持与失败范围 |
| L01 | 仓库、产物和依赖来源检查 | 无参考项目实现；新增依赖许可与声明齐全 |
